<!DOCTYPE html>
<link rel="icon"
      type="image/png"
      href="/img/favicon.ico" />
<html>
  <head>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
      <div class="btn-group">
      <a href="{{ .glink }}" text-decoration:none><button>Login with Google!</button></a>
      <a href="{{ .flink }}" text-decoration:none><button>Login with facebook!</button></a>
      </div>
  </body>
</html>
